package TP14;

import io.jbotsim.core.Node;

public class Cow extends Node {
    //static Node= farmer;
   /* int speed =0;
    public Cow() {
        setIcon("/link /to /cow /icon");
        setIconSize(30);
        disableWireless();

    }
    @Override
    public void onClock(){
        if(speed > 0){
            move(speed --);
        else
            if (speed == 0 this.disableWireless();)
        }
    }*/
}
